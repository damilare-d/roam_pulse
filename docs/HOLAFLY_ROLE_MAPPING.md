# Holafly Senior Flutter Developer — Requirement Mapping

Status: living document, updated at the end of every phase (§50
Definition of Done requires it) — currently through Phase 10. Doubles as a
progress record and interview leave-behind.

Legend: 🔲 planned · 🟡 in progress · ✅ done (tests + docs included)

| Requirement | RoamPulse feature | Implementation | Tests | Demo evidence | Status |
|---|---|---|---|---|---|
| Product discovery | Problem framing, user journeys | `docs/PRODUCT_DISCOVERY.md` | — | This document set | ✅ (Phase 0) |
| Modular / monorepo architecture | `packages/*` workspace | Dart pub workspace + Melos; `core`, `network`, `design_system`, `storage`, `connectivity`, `plans`, `diagnostics` now have real implementations, not just scaffolds | `flutter pub get`/`melos run analyze`/`melos run test` pass across all 9 packages + app | Repo tree walkthrough | 🟡 |
| Flutter architecture (clean layering) | Presentation → BLoC → Repository → DataSource | `DashboardPage`/`DemoEntryPage`/`ConnectivityCard`/`ConnectionDetailsPage` → Bloc → Repository → `ApiClient`; `AuthGuard` (`auto_route`) protects both `DashboardRoute` and `ConnectionDetailsRoute`; `connectivity` and `plans` are feature domains living in their own packages rather than app-local (as ADR-001 promised for Phase 7); `DashboardPage` renders its cards as independent siblings so one card's failure can't hide another's working (cached) data (see ADR-001, ADR-006, ADR-010) | `dashboard_bloc_test.dart`, `dashboard_page_test.dart`, `auth_bloc_test.dart`, `auth_repository_test.dart`, `demo_entry_page_test.dart`, `connectivity_bloc_test.dart`, `connectivity_test.dart`, `connectivity_card_test.dart`, `connection_details_page_test.dart`, `caching_connectivity_repository_test.dart`, `cache_test.dart`, `plans_test.dart` (all zero network access) | Live Windows desktop run: splash → demo entry → full dashboard (trip, usage, connectivity, connection details) against the real backend | 🟡 |
| BLoC state management | `DashboardBloc` (Phase 3, expanded Phase 7 to parallel-fetch trip+plan+usage), `AuthBloc` (Phase 4), `ConnectivityBloc` (Phase 5), `DiagnosticsBloc` (Phase 8) shipped; `PlanBloc`/`UsageBloc` deliberately folded into `DashboardBloc` (§21: don't split blocs without a reason — see dashboard_bloc.dart's doc comment) rather than proliferating; only `SyncBloc` remains | `flutter_bloc`, sealed event/state classes; see ADR-002 | 16 unit tests across `DashboardBloc`/`AuthBloc`/`ConnectivityBloc`/`DiagnosticsBloc` (initial state, success path, per-dependency failure paths) | Live demo: splash → entry → full dashboard → connection details → run diagnostics → healthy result | 🟡 |
| Advanced networking | Dio client with error classification mirroring backend `apperror` codes; timeouts configured; `postJson`/`getJsonList` added for POST and list endpoints; `ApiClient.addInterceptor()` opens one seam for request-level fault injection (Chaos Mode, Phase 9) without exposing the underlying Dio instance | `packages/network` (`ApiClient`, `mapDioException`), `apps/mobile/lib/features/chaos/chaos_interceptor.dart` | 6 unit tests covering timeout/connection/NOT_FOUND/VALIDATION_ERROR/fallback-by-status/unknown, `network_test.dart` covers `addInterceptor` | Live HTTP round-trip (Dio log) against the Go backend, incl. parallel trip+plan+usage and connectivity+events requests; live Chaos Mode run (see Error handling row) exercises the interceptor seam itself | 🟡 |
| Advanced caching / offline-first | `Cache<T>`/`Cached<T>` (TTL, staleness, timestamps) + `CachingConnectivityRepository` (network-first, cache fallback — see ADR-006 for why not full SWR) | `packages/storage` (`Cache<T>`), `packages/core` (`Cached<T>`), `packages/connectivity` (`CachingConnectivityRepository`); UI never shows stale data unmarked (`ConnectivityCard`'s stale banner) | `cache_test.dart` (TTL/staleness/overwrite/clear), `caching_connectivity_repository_test.dart` (fresh / stale-fallback / no-cache-to-fall-back-on), `connectivity_card_test.dart` (stale banner rendering) | Live: primed cache online, killed backend, relaunched — cached SoftBank data shown with stale banner instead of an error; restarted backend, tapped refresh — banner cleared, fresh data returned (§27 scenario 2/3, confirmed live) | ✅ (connectivity only; other repos adopt the same pattern as built) |
| Native Android integration | Home-screen App Widget (Jetpack Glance) | `apps/mobile/android/.../widget/` (`RoamPulseWidget`, `RoamPulseWidgetReceiver`, `WidgetDataRepository`, pure `WidgetTransform.kt`), fed by `NativeWidgetService`/`MethodChannelNativeWidgetService` (`apps/mobile/lib/features/native_widget/`) via a `"com.roampulse.widget"` MethodChannel — see ADR-007 | `widget_data_test.dart`/`method_channel_native_widget_service_test.dart` (Dart contract), `WidgetTransformTest.kt` (9 cases, plain JUnit, no Robolectric — same pure-core pattern as ADR-009/Chaos Mode) | Live: `:app:assembleDebug` succeeded; widget added to a physical Android 14 device's home screen, rendered real SoftBank/5G/Tokyo connectivity + plan data from the actual backend, and updated in place after an in-app refresh | ✅ |
| Native iOS integration | WidgetKit widget (SwiftUI, `StaticConfiguration`) | `apps/mobile/ios/RoamPulseWidget/` — structurally identical to Android field-for-field (`WidgetTransform.swift`, `WidgetDataStore.swift` over a shared App Group `UserDefaults` suite, `RoamPulseWidget.swift`); `AppDelegate.swift` adds the matching MethodChannel handler | None run — no macOS/Xcode available in this environment; manually traced against `WidgetTransformTest.kt`'s cases only | Not build-verified; `apps/mobile/ios/RoamPulseWidget/README.md` documents the exact manual Xcode steps (add Widget Extension target, target membership, App Group entitlement) to finish and verify it on a Mac | 🟡 (written, honestly unverified — see ADR-007) |
| High-performance UI | Parallel dashboard API loading, rebuild discipline | `ConnectivityBloc` fetches status + recent events concurrently; `DashboardBloc` fetches trip + plan + usage concurrently — all futures started before any is awaited, rather than sequential round trips | `connectivity_bloc_test.dart` and `dashboard_bloc_test.dart` cover all-succeed and per-dependency-fails cases | Dio log shows `/trips/current`, `/plans/current`, `/usage` (and separately `/connectivity`, `/connectivity/events`) firing before any resolves | 🟡 |
| Unit / widget / integration testing | Full pyramid per §25–27 | Across all packages | — | CI test reports | 🔲 |
| Test-first development | Behaviour → tests → implementation sequence | The diagnostic engine (`Diagnose`) was written to a pre-written test file — all 9 table-driven cases passed on the first implementation attempt; a design flaw in an early "stale sync" rule was then caught by *live* testing against real seed data and corrected (see ADR-009). The same pure-core/thin-IO-shell pattern was reused for Chaos Mode's `decideChaosAction` (Phase 9, Dart) and again for the native widgets' `mapToWidgetUiState` (Phase 10, Kotlin — `WidgetTransformTest.kt` written and passing before the Glance UI shell around it) | `diagnostics_engine_test.go`/`diagnostics_service_test.go` (Go), `chaos_action_test.dart` (Dart), `WidgetTransformTest.kt` (Kotlin) | ADR-009's "course-correction" section documents the diagnostics example end-to-end; the same pattern now has three independent, live-verified instances across three languages (Go, Dart, Kotlin) | ✅ (three independent instances of the same process) |
| AI-powered product workflow | Connectivity Recovery Agent, with the deterministic diagnostic engine (§15/ADR-009's non-AI half) built and shipped first so the AI has a real fallback and a shared results table to write into, not a design retrofitted later | Deterministic half done: `backend/api/internal/service/diagnostics_engine.go` (pure, no I/O) + `DiagnosticService` (gathers plan/eSIM/session state, persists to `diagnostic_sessions`/`diagnostic_results`, tagged by engine) + `packages/diagnostics` (Flutter consumer). AI half (Phase 11): `packages/ai_agent` + Go AI orchestration | Deterministic: 9 engine + 5 service tests (Go), 5 model/bloc + 3 widget tests (Dart). AI: mocked-Claude tests, schema validation tests (Phase 11) | Live: `POST /api/v1/diagnostics` — "Everything looks good" (healthy) and "Network unavailable" (disconnected session) both verified; full UI flow (connectivity card → connection details → run diagnostics) confirmed live. AI diagnostic demo (§27 scenario 5) is Phase 11 | 🟡 (deterministic half done; AI half is Phase 11) |
| AI-assisted engineering workflow | `/tools/ai` proposal → validate → generate → test pipeline | `tools/ai` scripts | Guardrail tests (AI can't skip analysis/tests) | `docs/AI_DEVELOPMENT_WORKFLOW.md` | 🔲 |
| CI/CD | Per-surface GitHub Actions | `flutter-ci.yml`, `backend-ci.yml` live on GitHub; `integration.yml` added in Phase 14 once `integration_test/` exists | CI itself is the test | Both workflows green on `main` (github.com/damilare-d/roam_pulse/actions) | 🟡 |
| Observability | Latency, sync duration, cache hit/miss, connectivity transitions, AI outcome logging | `packages/analytics` + backend structured logs | Log-emission unit tests | Dev console log sample | 🔲 |
| Error handling | `AppFailure` hierarchy (Flutter) mirrors `apperror` codes (Go); every failure mode is reproducible on demand via a developer-only Chaos Mode console (offline, timeout, slow network, 500, malformed/empty response, cache clear/expire) rather than relying on flaky real-world conditions to demo resilience | `packages/core` + `packages/network`'s `mapDioException` + Go error envelope; `apps/mobile/lib/features/chaos/` (`ChaosModeController`, `ChaosInterceptor`, `ChaosModePage`) wired behind `kDebugMode` so none of it exists in a release build | Failure-mapping unit tests both sides (Go `apperror_test.go`, Dart `network_test.dart`); `chaos_action_test.dart` (9 cases, pure decision function) | Error-state widget demo (`ErrorView` + retry); live Chaos Mode run: offline → cached/stale data or error view (not silent success), 500 → server-error view, malformed response → graceful parsing error (no crash), cache clear/expire both behave as documented — all confirmed live against the running app | ✅ |
| Technical documentation | ADRs, architecture docs, API docs | `docs/` | — | This document set | 🟡 |
| Architecture decisions | ADR-001..010 | `docs/decisions/`: ADR-001 (Flutter layering), ADR-002 (BLoC), ADR-003 (monorepo), ADR-004 (Go backend), ADR-005 (PostgreSQL), ADR-006 (offline-first caching), ADR-007 (native widgets), ADR-009 (deterministic diagnostics before AI), ADR-010 (demo auth) written; ADR-008 (Claude agent) lands with Phase 11 | — | ADR walkthrough | 🟡 |
| Long-term codebase health | Package ownership boundaries, lint discipline, no dead abstractions | Ongoing | Static analysis in CI | Codebase tour | 🔲 |
