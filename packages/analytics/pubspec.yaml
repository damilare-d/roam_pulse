name: analytics
description: "Lightweight observability hooks: latency, cache hit/miss, connectivity transitions."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.0

resolution: workspace

dev_dependencies:
  test: ^1.25.0
  lints: ^5.0.0
