name: core
description: "Shared failures, result types, env/config, and logging contracts used across RoamPulse packages."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.0

resolution: workspace

dev_dependencies:
  test: ^1.25.0
  lints: ^5.0.0
