/// Deterministic connectivity diagnostic engine.
///
/// Scaffolded in Phase 1 (monorepo foundation). Implementation lands
/// alongside the features that first need it — see docs/PRODUCT_DISCOVERY.md.
library;
