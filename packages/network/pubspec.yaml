name: network
description: "Dio-based HTTP client, interceptors, and structured error classification."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.0

resolution: workspace

dependencies:
  core:
    path: ../core
  dio: ^5.7.0

dev_dependencies:
  test: ^1.25.0
  lints: ^5.0.0
