# AI development workflow tooling

Placeholder — implemented in Phase 12 per `docs/PRODUCT_DISCOVERY.md` and
`docs/ARCHITECTURE.md`.

This directory will hold the developer-facing AI workflow scripts described
in section 18 of the build brief: feature proposal → architecture
validation → code generation → formatting → static analysis → tests →
coverage → human review. AI-assisted generation is not allowed to skip any
of those gates.
